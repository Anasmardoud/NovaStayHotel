<UserControl x:Class="NovaStayHotel.Views.HomeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:telerik="http://schemas.telerik.com/2008/xaml/presentation"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="1000">

    <UserControl.Resources>
        <!-- Gradient Brushes -->
        <LinearGradientBrush x:Key="BackgroundGradient" StartPoint="0,0" EndPoint="1,1">
            <GradientStop Color="#F8F9FA" Offset="0"/>
            <GradientStop Color="#E9ECEF" Offset="1"/>
        </LinearGradientBrush>

        <LinearGradientBrush x:Key="PrimaryGradient" StartPoint="0,0" EndPoint="1,1">
            <GradientStop Color="#4F46E5" Offset="0"/>
            <GradientStop Color="#7C3AED" Offset="1"/>
        </LinearGradientBrush>

        <!-- Button Style -->
        <Style x:Key="NavigationButton" TargetType="Button">
            <Setter Property="Background" Value="{StaticResource PrimaryGradient}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="40,20"/>
            <Setter Property="Margin" Value="15"/>
            <Setter Property="FontSize" Value="18"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="MinWidth" Value="200"/>
            <Setter Property="MinHeight" Value="80"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="12"
                                Padding="{TemplateBinding Padding}">
                            <Border.Effect>
                                <DropShadowEffect ShadowDepth="3" BlurRadius="10" Opacity="0.2" Color="Black"/>
                            </Border.Effect>
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background">
                                    <Setter.Value>
                                        <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                            <GradientStop Color="#6366F1" Offset="0"/>
                                            <GradientStop Color="#8B5CF6" Offset="1"/>
                                        </LinearGradientBrush>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background">
                                    <Setter.Value>
                                        <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                            <GradientStop Color="#3730A3" Offset="0"/>
                                            <GradientStop Color="#5B21B6" Offset="1"/>
                                        </LinearGradientBrush>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid Background="{StaticResource BackgroundGradient}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Ribbon -->
        <telerik:RadRibbonView Grid.Row="0">
            <telerik:RadRibbonTab Header="Home">
                <telerik:RadRibbonGroup Header="Navigation">
                    <telerik:RadRibbonButton Text="Reservations" 
                                           Command="{Binding NavigateToReservationListCommand}" 
                                           Size="Large"/>
                    <telerik:RadRibbonButton Text="Guests" 
                                           Command="{Binding NavigateToGuestListCommand}" 
                                           Size="Large"/>
                    <telerik:RadRibbonButton Text="Rooms" 
                                           Command="{Binding NavigateToRoomListCommand}" 
                                           Size="Large"/>
                </telerik:RadRibbonGroup>
            </telerik:RadRibbonTab>
        </telerik:RadRibbonView>

        <!-- Simple Content with Navigation Buttons -->
        <Grid Grid.Row="1">
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                <!-- Title -->
                <TextBlock Text="NovaStay Hotel Management" 
                           FontSize="36" 
                           FontWeight="Bold" 
                           HorizontalAlignment="Center" 
                           Foreground="#1F2937"
                           Margin="0,0,0,40"/>

                <!-- Navigation Buttons -->
                <WrapPanel HorizontalAlignment="Center" Orientation="Horizontal">
                    <Button Content="Manage Reservations" 
                            Style="{StaticResource NavigationButton}"
                            Command="{Binding NavigateToReservationListCommand}"/>

                    <Button Content="Manage Guests" 
                            Style="{StaticResource NavigationButton}"
                            Command="{Binding NavigateToGuestListCommand}"/>

                    <Button Content="Manage Rooms" 
                            Style="{StaticResource NavigationButton}"
                            Command="{Binding NavigateToRoomListCommand}"/>
                </WrapPanel>

                <TextBlock Text="Select an option above to get started" 
                           FontSize="16" 
                           HorizontalAlignment="Center"
                           Foreground="#6B7280"
                           Margin="0,30,0,0"/>
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>